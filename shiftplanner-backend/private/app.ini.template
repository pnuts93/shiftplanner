# Make sure to protect this directory from access via webserver
DOMAIN=

DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASS=

SMTP_HOST=
SMTP_PORT=
SMTP_EMAIL=
SMTP_PASSWORD=
NO_REPLY_EMAIL=
MAX_LOGIN_ATTEMPTS=
ENV=
DEFAULT_LOCALE=

session.use_cookies=1
session.use_only_cookies=1
session.cookie_lifetime=0
session.cookie_path="/"
session.cookie_domain=
session.cookie_secure=1
session.cookie_httponly=1
session.cookie_samesite="Strict"